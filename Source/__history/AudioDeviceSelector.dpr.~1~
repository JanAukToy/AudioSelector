program AudioSelector;

uses
  Vcl.Forms,
  Form_Main in 'Form_Main.pas' {FormMain},
  cls_SoundDevices in 'cls_SoundDevices.pas',
  myMMDeviceAPI in 'myMMDeviceAPI.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TFormMain, FormMain);
  Application.Run;
end.
